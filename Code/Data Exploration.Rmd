---
title: "Data Exploration"
author: "Candela Cerpa & Samantha Pace & Vinny Whatley"
date: "2023-11-20"
output: pdf_document
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
#install.packages("tidyverse") # This code chunk installs the tidyverse package.
#install.packages("lubridate") # This code chunk install lubridate package.
#install.packages("here") # This code chunk will install the here package.
# they are all now comments so it will knit.

library(tidyverse)
library(lubridate)
library(here)

# Working Directory with the here() command
here()
```

## Exploratory Visualizations 

```{r visualizations}
ggplot(PhoenixAZ_processed, 
       aes(x = Date, y = Daily_Temp_Max)) +
         geom_line() + 
  geom_smooth (method = lm)+
  labs( title = "Phoenix Daily Maximum Temperatures 1980-2022")

Three_cities <- rbind(LasVegasNV_processed, 
                      PhoenixAZ_processed, 
                      SanDiegoCA_processed)

ggplot(Three_cities, aes(x = Date, y = Daily_Temp_Max, color = Location)) +
  geom_line() +
  geom_smooth(method=lm)
```


```{r pressure, echo=FALSE}

```

